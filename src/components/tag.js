const Tag = ({ children }) => (
  <span className="inline-flex items-center px-2 py-1 mr-2 rounded text-sm font-medium bg-gray-100 dark:bg-gray-800 dark:text-gray-300">
    {children}
  </span>
);

export default Tag;
