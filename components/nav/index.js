import React from "react";
import classes from "./nav.module.css";

const Nav = props => (
  <div className={classes.Nav}>
    <h6 className={classes.Name}>Sam Potter</h6>
  </div>
);

export default Nav;
